# Uncomment the next line to define a global platform for your project
# platform :ios, '9.0'

target 'ArgentAssessment' do
  # Comment the next line if you're not using Swift and don't want to use dynamic frameworks
  use_frameworks!
  pod 'web3.swift'
  pod 'EasyPeasy'
  pod 'SwiftKeychainWrapper'

  target 'ArgentAssessmentTests' do
    inherit! :search_paths
    # Pods for testing
	pod 'Quick'
	pod 'Nimble'
  end

  target 'ArgentAssessmentUITests' do
    inherit! :search_paths
    # Pods for testing
  end

end
